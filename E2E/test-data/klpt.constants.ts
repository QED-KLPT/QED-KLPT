/**
 * Shared KLPT Test passkey — not a per-user secret, so it's hardcoded here
 * rather than sourced from .env. Import it wherever it's needed instead of
 * duplicating the value.
 */
export const KLPT_PASSKEY = 'klpt';
